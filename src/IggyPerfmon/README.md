# IggyPerfmon
`iggyperfmon` provides the iggy perfmon and explorer, the explorer allows external debugging tools to connect to iggy and view the display hierarchy and other information about the runtime. Perfmon allows tracking the runtimes performance such as memory usage.
